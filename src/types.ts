export type Project = {
  name: string;
  tech: string;
  desc: string;
  color: string;
  imageUrl?: string;
  link?: string
};

export type TechTags = {
  label: string;
  bgColor: string;
  textColor: string;
};
